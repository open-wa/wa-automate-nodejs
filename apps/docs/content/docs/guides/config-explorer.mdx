---
title: "Config Explorer"
description: "Every open-wa config option in three forms: wa.config.json, CLI flags, and WA_ environment variables."
icon: "Sliders"
---

# Config Explorer

open-wa has one configuration schema, and every option can be set three ways:

- in a config file (`wa.config.json` / `wa.config.mjs`), by its key
- as a CLI flag (`--kebab-case`)
- as an environment variable (`WA_UPPER_SNAKE`)

Use the explorer below to search every option, filter by group, and switch
between the three forms. The table and the copy-paste snippet update to match
the format you pick. Options are grouped by area (browser, session, server,
logging, and so on) — the groups are inferred from the config schema itself, so
they always match the source.

<ConfigExplorer />

<Callout type="info">
Nested options (like `mcp.enabled`) are set in the config file or via env vars
(`WA_MCP_ENABLED`); they do not have a dedicated CLI flag. A few flags also have
short aliases (for example `-p` for `--port`) — the canonical `--kebab-case`
form shown here always works.
</Callout>

This page is generated from `@open-wa/config`'s schema, so it always matches the
options the runtime actually accepts.
