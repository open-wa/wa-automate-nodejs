---
title: Pricing and licensing
description: Decide whether your open-wa workflow needs a license key, then use the purchase or support path for current terms.
---

# Pricing and licensing

Use this page to decide whether you need a license key.

If you already know the gated method or runtime behavior you need, read
**[Licensed features](/docs/licensing/licensed-features)** for the operational
details before you buy.

<Callout type="note" title="Prices live in the purchase flow">
  Prices are shown in the purchase flow and may change. Use the license key page
  for the current purchase options shown there.
</Callout>

<div className="my-6 flex flex-wrap gap-3">
  <GetLicenseButton />
  <a
    href="https://openwa.page.link/key"
    className="inline-flex items-center justify-center rounded-full border border-fd-border bg-fd-card px-5 py-2.5 text-sm font-medium text-fd-foreground hover:bg-fd-accent hover:text-fd-accent-foreground"
  >
    Open license key page
  </a>
</div>

## First decision: do you need a key?

Start from the method or integration your workflow calls. The current docs mark
licensed methods as `restricted`, `insiders`, or `none` in the generated method
metadata. Methods marked `none` are not license-gated in the schema.

<ComparisonTable
  columns={[
    { key: 'answer', label: 'Decision' },
    { key: 'nextStep', label: 'Next step' },
  ]}
  rows={[
    {
      feature: 'Your workflow only uses methods marked `none`',
      values: {
        answer: 'You do not need a license key for that method based on the current schema.',
        nextStep:
          'Run the workflow normally, then keep the generated API reference for your deployed version as the source of truth.',
      },
    },
    {
      feature: 'Your workflow calls a `restricted` method',
      values: {
        answer: 'You need a restricted license key before you rely on that gated behavior.',
        nextStep:
          'Buy or request the key through the license key page, then validate it against the host account and session you will run.',
      },
    },
    {
      feature: 'Your workflow calls an `insiders` method',
      values: {
        answer: 'You need insiders access before you rely on that gated behavior.',
        nextStep:
          'Use the purchase flow for the current insiders option, then test the exact method in the real runtime session.',
      },
    },
    {
      feature: 'You are unsure which method an integration calls',
      values: {
        answer: 'Do not buy based on the integration name alone.',
        nextStep:
          'Map the integration to the exact method or runtime behavior first, then decide from the licensed-feature metadata.',
      },
    },
  ]}
/>

## Current gated methods

The current licensed-feature page lists these gated methods. Treat this as a
buying checklist, not a promise that every past or future build uses the same
mapping.

<ComparisonTable
  columns={[
    { key: 'tier', label: 'License tier' },
    { key: 'methods', label: 'Methods listed in the current docs' },
  ]}
  rows={[
    {
      feature: 'Restricted',
      values: {
        tier: '`restricted`',
        methods: '`createGroup`, `joinGroupViaLink`, and `joinGroup`',
      },
    },
    {
      feature: 'Insiders',
      values: {
        tier: '`insiders`',
        methods:
          '`getNumberProfile`, `checkNumberStatus`, `getOrder`, status and story methods, button and list message methods, starred message access, and profile picture methods listed on the licensed-feature page.',
      },
    },
  ]}
/>

<Callout type="tip" title="Buy for the runtime you will operate">
  A key is part of the runtime launch path. Validate it with the host account,
  session, deployment clock, and exact gated method you plan to use in production.
</Callout>

## What a license key changes at runtime

<Steps>
  <Step title="You provide the key at launch">
    Supply the key through runtime config or the Easy API / CLI `--license-key`
    option.
  </Step>
  <Step title="The runtime checks the key">
    When the host account context is available, the runtime sends the key to the
    validation service and classifies the result before the client is marked
    ready.
  </Step>
  <Step title="The gated method still needs testing">
    A valid key is not a substitute for testing. Confirm the exact restricted or
    insiders method works in the same session and deployment you plan to run.
  </Step>
</Steps>

## Integration buyers

Integrations do not need a separate license just because they are integrations.
The license decision follows the behavior the integration calls.

<ComparisonTable
  columns={[
    { key: 'licenseQuestion', label: 'License question' },
    { key: 'buyerAction', label: 'Buyer action' },
  ]}
  rows={[
    {
      feature: 'Webhooks',
      values: {
        licenseQuestion:
          'Does the webhook only receive normal runtime events, or does the workflow also call a gated method?',
        buyerAction:
          'If it only receives ungated events, decide from the underlying method list. If it calls restricted or insiders behavior, buy for that gated behavior.',
      },
    },
    {
      feature: 'Chatwoot',
      values: {
        licenseQuestion:
          'Which open-wa methods does the Chatwoot bridge need for your support workflow?',
        buyerAction:
          'Buy only if the bridge depends on restricted or insiders methods in your real workflow.',
      },
    },
    {
      feature: 'Node-RED',
      values: {
        licenseQuestion:
          'Which Easy API methods do your Node-RED nodes call?',
        buyerAction:
          'Match those methods to the licensed-feature metadata before buying.',
      },
    },
    {
      feature: 'MCP',
      values: {
        licenseQuestion:
          'Which Easy API tools will the MCP client execute?',
        buyerAction:
          'MCP exposes Easy API methods as tools, so the same method-level license decision applies.',
      },
    },
    {
      feature: 'Plugin SDK',
      values: {
        licenseQuestion:
          'Which methods or runtime behavior does the plugin call after it loads?',
        buyerAction:
          'A plugin wrapper does not decide the tier. The called behavior does.',
      },
    },
  ]}
/>

## Team and organization buying

The current source-backed path for team or organization questions is the public
license key page plus the project support paths. The docs do not define
multi-seat terms, per-developer scope, customer deployment scope, transfer rules,
or support service levels.

If more than one person, account, environment, or customer deployment is involved,
do not assume the scope. Use the license key page first, then use Discord or the
paid consult/support links from the project README if the purchase flow does not
answer your buying question.

## Before you buy

<ComparisonTable
  columns={[
    { key: 'check', label: 'Check' },
    { key: 'why', label: 'Why it matters' },
  ]}
  rows={[
    {
      feature: 'Exact method or behavior',
      values: {
        check: 'Name the restricted or insiders method your workflow depends on.',
        why: 'The current licensing guidance is method-level, not page-name-level.',
      },
    },
    {
      feature: 'Runtime session',
      values: {
        check: 'Know which host account and session will run the workflow.',
        why: 'The licensed-feature docs tell readers to validate against the real host-account context.',
      },
    },
    {
      feature: 'Deployment path',
      values: {
        check: 'Know whether the workflow runs through Easy API, custom code, MCP, Chatwoot, Node-RED, webhooks, or a plugin.',
        why: 'The wrapper can change the setup steps, but the license decision still follows the gated behavior.',
      },
    },
    {
      feature: 'Buying scope',
      values: {
        check: 'For team or organization use, confirm unanswered scope questions through purchase or support.',
        why: 'The local sources do not publish team-seat or organization-license terms.',
      },
    },
  ]}
/>

## Buy or verify first

<div className="my-8 flex flex-wrap gap-3">
  <GetLicenseButton />
  <a
    href="/docs/licensing/licensed-features"
    className="inline-flex items-center justify-center rounded-full border border-fd-border bg-fd-card px-5 py-2.5 text-sm font-medium text-fd-foreground hover:bg-fd-accent hover:text-fd-accent-foreground"
  >
    Read licensed-feature operations
  </a>
  <a
    href="https://discord.gg/dnpp72a"
    className="inline-flex items-center justify-center rounded-full border border-fd-border bg-fd-card px-5 py-2.5 text-sm font-medium text-fd-foreground hover:bg-fd-accent hover:text-fd-accent-foreground"
  >
    Ask in Discord
  </a>
</div>

<div className="my-8 grid gap-4 md:grid-cols-2">
  <LicensedFeatureCallout tier="restricted" title="Need a restricted method?">
    Use the restricted path when your workflow depends on a method currently
    marked `restricted`, then validate the key in the session you will run.
  </LicensedFeatureCallout>
  <LicensedFeatureCallout tier="insiders" title="Need an insiders method?">
    Use the insiders path when your workflow depends on a method currently marked
    `insiders`, then test that exact method before relying on it.
  </LicensedFeatureCallout>
</div>

## Frequently asked questions

<FAQ
  items={[
    {
      question: 'Where are the prices?',
      answer:
        'Prices are shown in the purchase flow and may change. Open the license key page for the current purchase options.',
    },
    {
      question: 'Can I decide from an integration name like webhooks, Chatwoot, Node-RED, MCP, or plugin SDK?',
      answer:
        'No. Decide from the exact method or runtime behavior the integration calls. Webhooks, Chatwoot, Node-RED, MCP, and plugin SDK workflows only need a key when the behavior they rely on is marked restricted or insiders.',
    },
    {
      question: 'What if I need team or organization licensing?',
      answer:
        'Use the license key page first. If the purchase flow does not answer your team or organization scope question, use Discord or the paid consult/support links from the README. The docs do not publish multi-seat terms.',
    },
    {
      question: 'What should I read before buying?',
      answer:
        'Read [Licensed features](/docs/licensing/licensed-features), identify the exact gated method, then validate the key in the real host-account session after purchase.',
    },
  ]}
/>

<Callout type="tip" title="Best next step">
  If you are unsure, identify the exact method your workflow needs. If that still
  leaves a buying-scope question, use the purchase or support path instead of
  guessing.
</Callout>
