---
title: Licensed features
description: What licensed features are for, how keys are used, and where to validate unlock behavior.
---

# Licensed features

Some functionality in the historical open-wa surface has been license-gated. The old docs mixed feature marketing, purchasing instructions, and implementation details in one page; this version separates the intent from the operational guidance.

<Callout type="info" title="Need the decision page instead of the technical explanation?">
  If you are trying to decide which license tier you need, read the dedicated
  **[Pricing and licensing](/docs/licensing/pricing)** page first, then come back
  here for the operational details.
</Callout>

<LicensedFeatureCallout tier="restricted" title="Need a key before you test a gated flow?">
  Use the public license path first, then validate the specific restricted or
  insiders behavior in the host-account session you actually plan to run.
</LicensedFeatureCallout>

## What licensing affects

Historically, license-gated behavior has included features such as:

- certain advanced or insiders-only capabilities
- restricted flows like messaging non-contact numbers
- premium/support-tier unlocks tied to a host account

Historically, the biggest source of confusion was assuming a key unlocked behavior globally. In practice, key behavior is usually tied to the session or host-account context you are actually running.

## How keys are supplied

You can provide a license key in the runtime config:

```ts
await create({
  licenseKey: 'YOUR-LICENSE-KEY',
});
```

Or via the Easy API / CLI path:

```bash
npx @open-wa/wa-automate --license-key "YOUR-LICENSE-KEY"
```

## Common license workflow

<Steps>
  <Step title="Acquire the right key for the real use case">
    Match the key to the host account or operational use case you actually plan
    to run, not just a temporary dev session.
  </Step>
  <Step title="Supply the key at launch">
    Provide the key through config or CLI so the runtime evaluates availability
    in the same session you care about.
  </Step>
  <Step title="Verify the session sees the license">
    Confirm the runtime recognizes the key in the target session before you try
    a gated flow.
  </Step>
  <Step title="Test the gated feature directly">
    Do not assume success from configuration alone. Test the exact restricted or
    insiders behavior you are relying on.
  </Step>
</Steps>

## Operational advice

- Treat license configuration as deploy-time secret material.
- Validate the license against the actual host account you intend to run.
- If unlock behavior looks inconsistent, verify the host number, deployment clock, and current runtime mode before assuming the key itself is wrong.

## If the key looks wrong

Check these first:

- the host account number associated with the running session
- whether the feature is actually available in the runtime surface you are using
- whether the machine clock and deployment environment are sane
- whether you are testing the gated feature through the same session the key is meant for

<FAQ
  items={[
    {
      question: 'Why does the key appear valid but the feature still looks locked?',
      answer:
        'The most common causes are testing the wrong host-account session, assuming the feature is available in the runtime surface you are using, or not validating the exact gated behavior directly.',
    },
    {
      question: 'Should I configure the key only in production?',
      answer:
        'No. Put the key into the same environment where you are validating the feature so you can confirm availability before rollout.',
    },
  ]}
/>

## Scope note

Feature availability has changed over time. Use the current generated API reference and runtime behavior as the source of truth for what is available in the build you are actually deploying.
