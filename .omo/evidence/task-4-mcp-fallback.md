# Task 4 MCP fallback

Result: pass.

`mcp.mdx` shows the source-backed `wa.config.mjs` path, states that `--mcp` is not parsed by the current source, and includes troubleshooting for unsupported `--mcp`, missing API key, 401/403, missing tools, and session-not-ready tool calls.
