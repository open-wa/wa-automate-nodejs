# Task 5 AI runnable examples

Result: pass.

`ai-agent-patterns.mdx` states that snippets marked runnable include setup/stubs and snippets marked pattern are structural. It defines local `callLLM`, `callVisionLLM`, and `callSTT` stubs, and includes `npm install bottleneck` / `npm install p-queue` where those packages are used.
