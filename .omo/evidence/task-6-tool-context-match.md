# Task 6 ToolContext docs match

Result: pass.

`plugins/ai-tools.mdx` and `plugins/hooks-reference.mdx` document `sessionId`, `logger`, and `abort`, and explicitly state that ToolContext does not include `client`.
