# Task 1 Easy API preservation

Result: pass.

Evidence:
- `apps/docs/content/docs/getting-started/easy-api.mdx` still exists.
- `apps/docs/content/docs/getting-started/meta.json` keeps `easy-api` immediately after `quickstart` and `v5-alpha`.
- `quickstart.mdx` links to deeper guides for next steps instead of replacing Easy API operations content.
