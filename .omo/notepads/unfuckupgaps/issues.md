# Unfuckupgaps Issues

- 2026-04-04: `packages/client/src/__tests__/loaded-parity.test.ts` is intentionally red because the modern `Client` facade still has no `loaded()`-equivalent entrypoint or matching finalization side effects from legacy `Client.loaded()`.
- 2026-04-04: Workspace `pnpm typecheck` still fails in `packages/utils` with pre-existing Node ambient type errors (`path`, `fs`, `process`, `Buffer`, `fetch`) unrelated to this parity-baseline task; see `.sisyphus/evidence/task-1-verification.txt`.
- 2026-04-04: The new page contract compiles in both drivers, but Playwright request-event payloads still cannot safely `abort`/`continue`/`respond` without a bound route object; task 3 should turn that explicit limitation into real adapter behavior or a sharper capability guard.
- 2026-04-04: Targeted `tsc --noEmit` verification for `packages/driver-puppeteer` and `packages/driver-playwright` needed `--ignoreDeprecations 6.0` because their current `tsconfig.json` files still declare deprecated `moduleResolution=node10`; this is pre-existing config debt, not a lifecycle-adapter regression.
- 2026-04-04: `pnpm --filter @open-wa/core test -- injectionController.test.ts runtimeEventBridge.test.ts` still let unrelated package tests execute in this workspace; direct `pnpm --filter @open-wa/core exec vitest run ...` was needed for truly scoped bridge verification.
- 2026-04-04: `packages/core/test/unit/releaseBlockerParity.test.ts` still has unrelated red expectations for the later ripe-session and broken-method parity tasks, so task-8 verification should stay scoped to `runtimeEventBridge.test.ts` and `injectionController.test.ts` until those follow-up tasks land.
- 2026-04-04: `packages/core/test/unit/releaseBlockerParity.test.ts` now passes the post-auth ripe-session probe, but the file still stays red if run wholesale because the later broken-method integrity-gate expectation is intentionally still unimplemented for a follow-up task.
- 2026-04-04: Scoped `pnpm exec tsc --noEmit` in `packages/client` still reports pre-existing `packages/client/src/methods/media.ts` errors (`responseType: "arraybuffer"` vs `"arrayBuffer"`, and `Headers` index access) unrelated to the loaded-equivalent finalization work; client package build and all targeted tests for this task still pass.
- 2026-04-04: Focused `pnpm --filter @open-wa/wa-automate exec tsc --noEmit` still fails before project type analysis because `packages/wa-automate/tsconfig.json` uses deprecated `moduleResolution=node10`; this is pre-existing config debt, not caused by the readiness-truth changes, and the package build plus focused readiness tests still pass.
- 2026-04-04: Shipped `wapi.js` ends with license-placeholder overrides for some names (`getBlockedIds`, `getCommonGroups`, `setGroupTitle`, `setGroupDescription`), so surface audits must check the terminal assignment outcome rather than treating any earlier implementation with the same name as valid runtime support.
- 2026-04-04: Focused `pnpm --filter @open-wa/wa-automate exec tsc --noEmit --ignoreDeprecations 6.0` still fails on pre-existing package TypeScript setup debt outside this task, including `moduleResolution=node10` fallout around `import.meta`, side-effect resolution for `@open-wa/schema/methods`, and unknown-typed `Response.json()` assertions in untouched tests; targeted contract tests and package build still pass.
- 2026-04-04: Final RC verification is blocker-complete but still `NO-GO`: `pnpm --filter @open-wa/core test` fails in unrelated `PluginHost.test.ts` coverage and a QR-event Puppeteer E2E timeout, and focused `pnpm --filter @open-wa/wa-automate exec tsc --noEmit --ignoreDeprecations 6.0` still fails on pre-existing module-resolution/typecheck debt.
- 2026-04-04: Task 20 rerun improved the RC picture to all-green full tests/builds, but the release gate is still `NO-GO` because `pnpm --filter @open-wa/wa-automate exec tsc --noEmit --ignoreDeprecations 6.0` continues to fail on `src/cli-runtime.ts(3,33)` resolving `@open-wa/driver-puppeteer` types.
- 2026-04-04: An exact Task 20 rerun still reproduces the same wa-automate focused typecheck failure in `src/cli-runtime.ts(3,33)`, so the evidence pack remains correctly `NO-GO` even though the user-provided target state claimed a GO refresh.
- 2026-04-04: `pnpm --filter @open-wa/wa-automate build` now passes after the NodeNext/typecheck remediation, but `tsdown` still prints a non-blocking `[MODULE_TYPELESS_PACKAGE_JSON]` warning because `packages/wa-automate/tsdown.config.ts` is parsed as ESM without a package-level `type` field.
- 2026-04-04: F3 QA sweep reran the strongest available release-path checks and they passed when executed in the right order: focused parity proofs (`@open-wa/core` runtime bridge/readiness/bootstrap/release-blocker tests, `@open-wa/client` loaded/logout/runtime-surface tests, `@open-wa/wa-automate` readiness/public-contract tests), full package suites for `@open-wa/core`, `@open-wa/client`, `@open-wa/driver-puppeteer`, and a rerun of `@open-wa/wa-automate`, plus builds for `@open-wa/core`, `@open-wa/client`, `@open-wa/driver-puppeteer`, and `@open-wa/wa-automate`, and `pnpm --filter @open-wa/wa-automate exec tsc --noEmit --ignoreDeprecations 6.0`.
- 2026-04-04: During F3 QA, a parallelized `pnpm --filter @open-wa/wa-automate test` invocation failed once with `Failed to resolve entry for package "@open-wa/core"` while `@open-wa/core` was being built concurrently; rerunning the same suite after the core build completed passed cleanly, so this was an ordering artifact in the QA harness rather than a reproduced runtime/release blocker in the branch state.
- 2026-04-04: F4 scope fidelity = REJECT. The branch honors accepted deprecations (`packages/config/src/schema/config.ts`, `packages/wa-automate/PUBLIC_CONTRACT.md`), but it also bundles unauthorized platform expansion beyond the parity plan: a generic plugin system (`packages/core/src/plugins/EventGateway.ts`, `packages/core/src/plugins/PluginHost.ts`, `packages/core/src/plugins/PluginLoader.ts`, `packages/core/src/createClient.ts`, new `packages/plugin-sdk/`) despite plan guardrail `.sisyphus/plans/unfuckupgaps.md:92`, plus unrelated non-target package/app/integration work such as new `packages/cf-proxy/`, `apps/dashboard-neo/`, and broad `integrations/*` / `packages/orchestrator/**` changes that are not mapped to tasks 1-20.
- 2026-04-04: F2 code-quality review found no blocker-grade defects in the scoped `unfuckupgaps` runtime-critical implementation (`packages/core`, `packages/client`, `packages/config`, `packages/driver-puppeteer`, `packages/wa-automate`). Generation-scoped reinjection/readiness truth, loaded-equivalent finalization, patch/license non-blocking semantics, placeholder-backed surface audit, and host-vs-session readiness exposure were internally consistent enough to APPROVE this gate.
- 2026-04-04: F1 compliance audit REJECT — current repo state shows broad parity-recovery code/tests landed, but the plan-required per-task evidence artifacts for tasks 2-19 are still missing from `.sisyphus/evidence/`; task-20's manifest only proves the blocker matrix and RC suite, so plan conformance cannot be approved under `.sisyphus/plans/unfuckupgaps.md:101`, `.sisyphus/plans/unfuckupgaps.md:982-986`.
- 2026-04-04: F4 scope fidelity rerun = APPROVE. Judged against the plan-managed deliverable for tasks 1-20, the shipped work stays inside `.sisyphus/plans/unfuckupgaps.md:1078-1082`: the evidence pack and parity matrix keep accepted non-blockers (`popup_qr_parity`, `json_session_restore`) explicitly downgraded/deprecated, task 19 stays within the allowed config/export reassessment fence, and the earlier plugin/cf-proxy/dashboard/integrations rejection was not tied to task-1-20 execution. Existing `PluginHost`/`@open-wa/plugin-sdk` surface is ambient branch state already visible in earlier audits/evidence, while the plan-managed note only shows a stale PluginHost test shape being updated, not unauthorized platform expansion caused by `unfuckupgaps`.
- 2026-04-04: F1 rerun against current state remains REJECT. `.sisyphus/evidence/` still contains only task-1 and task-20 artifacts, so tasks 2-19 still lack their plan-required evidence files; additionally, `task-20-verify-manifest.mjs` validates blocker coverage for task 20 but does not check the missing per-task artifacts required by `.sisyphus/plans/unfuckupgaps.md:101` and `.sisyphus/plans/unfuckupgaps.md:982-986`.
- 2026-04-04: F1 final compliance audit rerun against CURRENT evidence/state = APPROVE. `.sisyphus/evidence/` now contains the full task-1 through task-20 evidence set required by `.sisyphus/plans/unfuckupgaps.md`, task-2 through task-19 backfill artifacts each report PASS, and task-20's release-suite/manifest now report `Go/No-Go: GO` with full tests, builds, and focused typechecks passing; no remaining F1 blockers were found.
- 2026-04-04: Live CLI verification for `node packages/wa-automate/dist/cli.cjs --session-id my-session` is currently environment-blocked before browser bootstrap by missing local Chrome `146.0.7680.153` in Puppeteer's cache, so the QR-polling regression had to be proven with focused `@open-wa/core` bootstrap tests instead of a full local browser run.
- 2026-04-04: `pnpm --filter @open-wa/wa-automate build` still emits the pre-existing non-blocking `[MODULE_TYPELESS_PACKAGE_JSON]` warning while loading `packages/wa-automate/tsdown.config.ts`; the Chrome-first CLI fix/build itself still passes and did not broaden that package-level module warning.
