- 2026-04-12: Active Socket.IO cleanup for this pass was limited to dead middleware, docs entry points, ad hoc test coverage, and active package manifests. Remaining `socket.io` references are confined to legacy packages, plan/decision docs, and package-lock files.
- 2026-04-12: Deleting `packages/logger/src/middleware/socket.ts` also required removing the `socketLogger` re-export from `packages/logger/src/index.ts`; otherwise the logger package would retain a broken public export.
- 2026-04-12: Deleting `docs/docs/get-started/socketmode.md` required follow-up doc cleanup in `docs/docs/intro.md` and `docs/docs/get-started/docker.md` to avoid a dead navigation link and stale `--socket` guidance.
- 2026-04-12: `integrations/node-red` no longer imports `@open-wa/api` directly; its active runtime dependency is `@open-wa/socket-client`, which now acts as a compatibility wrapper over the v5 HTTP RPC + SSE transport.
- 2026-04-12: `packages/plugin-sdk/src/types.ts` still had transport-specific SocketClient wording even though the runtime contract is transport-agnostic; comment-only cleanup there removes stale Socket.IO assumptions without changing the exposed API shape.
- 2026-04-12: Final F1 gap closure required one last documentation sweep in `packages/socket-client`: both the README and the `SocketClient` JSDoc needed to describe the real v5 transport (HTTP RPC + SSE) and stop instructing users to launch the EASY API with `--socket`.
- 2026-04-12: `packages/socket-client/src/SocketClient.ts` no longer treats legacy listener names like `onMessage` or `onAck` as HTTP RPC methods. `listen()` now maps those legacy names onto the current SSE event names locally, and reconnect only restores SSE bindings plus local callbacks instead of calling a nonexistent upstream listener-registration endpoint.
