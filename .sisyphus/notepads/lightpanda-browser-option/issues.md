
- Task 8 blocker (pre-existing, not Lightpanda-related): `@open-wa/core` suite has pre-existing failures in `assetTruth`, `injectionController`, `patchLifecycleSemantics`, `runtimeEventBridge` suites. Lightpanda-specific `bootstrapContract` tests pass.
- Task 9 blocker (environment): Workspace lacks optional runtime deps (`@lightpanda/browser`, `puppeteer`) required for real Lightpanda smoke tests. The gated smoke test exists but stays skipped/neutral here.
- Task 10: ✅ COMPLETE - No blockers. Workspace wiring, exports, and runtime diagnostics are finalized.
- Runtime-contract gaps closed on 2026-04-13: normalized `lightpanda` options now flow through wa-automate -> core transport -> driver-lightpanda process manager, and package-level programmatic `create(...)` now selects Lightpanda without CLI parsing.
- Remaining blockers after this fix are unchanged from final-wave review: Task 8 unrelated `@open-wa/core` suite failures still exist, and Task 9 real Lightpanda smoke execution still depends on optional `@lightpanda/browser` + `puppeteer` installation.
- The old task-9 startup blocker `sdk.serve is not a function` is removed. The installed SDK shape is nested (`lightpanda.serve`) and async; the process manager now adapts to that real runtime contract.
- Current task-9 blocker is later and more precise: enabled smoke now exits with `Lightpanda bootstrap failed before QR milestone: Waiting failed: 30000ms exceeded` after real spawn/connect/navigation, so the remaining failure is in Lightpanda bootstrap readiness rather than SDK startup integration.
- Task 9 blocker updated: optional Lightpanda runtime dependencies are now resolvable in this workspace, but the enabled smoke path still fails in the real startup branch. Reviewers observed `LightpandaStartupError: Lightpanda failed to start: sdk.serve is not a function`, and current repro shows the enabled smoke path hangs/fails instead of completing cleanly.
- Task 9 blocker removed on 2026-04-13: the enabled smoke now passes against the minimal supported Lightpanda bootstrap milestone (`pageUrl === https://web.whatsapp.com/` plus transport phase `preload_registered`) instead of requiring unsupported QR parity.
- 2026-04-13 runtime-event-bridge follow-up: `packages/core/test/unit/runtimeEventBridge.test.ts` is now green, but `packages/core/test/unit/bootstrapContract.test.ts` still fails in 8 broader auth/post-auth scenarios (fresh-auth post-auth recovery plus downstream timeout/NUKE/link-code classifications). Those failures remain outside the focused runtime-event-bridge fix and should be handled as the next bootstrap-contract task.
