2026-04-13: Added a shared nested `lightpanda` runtime option contract in `packages/driver-interface/src/types.ts` and threaded it through `packages/core/src/createClient.ts` and `packages/core/src/transport/Transport.ts` so programmatic and CLI-created clients can pass Lightpanda-specific startup fields end-to-end without widening the generic launch contract.
2026-04-13: Updated `Transport.initialize()` to call `driver.init({ logger: this.logger })` and forward `lightpanda` into `driver.launch(...)`, which restores the normal runtime path needed for Lightpanda chosen-port/version diagnostics to emit during real startup.
